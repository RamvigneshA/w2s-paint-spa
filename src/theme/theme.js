import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FFFFFF',
    },
    secondary: {
      main: '#9C27B0',
    },
  },
  typography: {
    fontFamily: 'Segoe UI',
  },
});

export default theme;
